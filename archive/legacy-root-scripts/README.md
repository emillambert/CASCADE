# Legacy Root Scripts

This directory contains retired root-level compatibility wrappers.

The MASFE-era wrappers were archived during the CASCADE rename cleanup. Current
CASCADE root wrappers still live at the repository root because README, docs,
and tests treat them as supported reviewer conveniences.

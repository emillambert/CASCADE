[pytest]
testpaths = tests
pythonpath = .
markers =
    slow: heavier end-to-end or plotting-oriented checks that are not part of the default fast suite
    validation: offline baseline checks against accepted reference artifacts
